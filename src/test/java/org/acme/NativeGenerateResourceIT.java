package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGenerateResourceIT extends GenerateResourceTest {

    // Execute the same tests but in native mode.
}
